'''programa para tirar informações dos numero'''

a = int(input('Primeiro: '))
b = int(input('Segundo: '))
c = int(input('Terceiro: '))

menor = a
if b < a and b < c:
    menor = b
if c < a and c < b:
    menor = c
maior = a
if b > a and b > c:
    maior = b
if c > a and c > b:
    maior = c

print('O menor valor digitado foi {} e o maior {}'.format(menor, maior))
